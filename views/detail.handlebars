
<div class="container">
    <h1>Detail Page</h1>
    <div class="row">
        <div class="col-md-6">
            <h2>Item Details</h2>
            {{!-- <p>Item ID: {{item.id}}</p> --}}
            <p>Item name: {{item.name}}</p>
            <p>Item price: {{item.category}}</p>
            <p>Item description: {{item.description}}</p>
            <img src="https://{{bucket}}.s3.amazonaws.com/{{item.id}}.png?{{addNumbersToSrc}}" alt="Item Image" class="img-fluid" style="max-width: 300px; max-height: 300px;">
            <br>
            <a href="/update/{{item.id}}" class="btn btn-warning" style="margin-top: 10px;">Edit</a>
            <a href="/list" class="btn btn-primary" style="float: right;">Back</a>
        </div>
    </div>
</div>
 